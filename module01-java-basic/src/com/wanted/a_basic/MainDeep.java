package com.wanted.a_basic;

public class MainDeep {

    /**
     * Deep Dive : JVM 메모리 구조
     * 1. Method Area (메서드 영역)
     * - 클래스에 대한 정보, static 변수, 상수 풀 저장하는 공간
     * 2. Heap (힙)
     * - 객체(Instance) 및 인스턴스 변수 저장
     * 3. Stack (스택)
     * - 지역 변수, 메서드 호출 시 생성되는 저장 공간
     * 4. PC Register
     * - 현재 실행 중인 JVM 명령어의 주소 저장
     * 5. Native Method Stack
     * - JVM에서 사용하는 네이티브 코드 실행의 공간
     * - OS 통신 홋은 고성능 연산과 같은 작업을 위해 자바보다
     * 적합한 언어로 개발 된 코드가 저장되는 공간
     */
}
